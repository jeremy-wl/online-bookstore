<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>


  <ul class="slider1">
    <% Product.all.each do |product| %>
          <li><%= image_tag(product.image_url, class:"slide") %></li>
          <li><%= image_tag(product.image_url, class:"slide") %></li>
          <li><%= image_tag(product.image_url, class:"slide") %></li>
    <% end %>
  </ul>


<h1><%= t('.title_list') %></h1>

  <% @products.each do |product| %>
      <div class="entry">
        <%= image_tag(product.image_url) %>
        <h3><%= product.title %></h3>
        <p><%= sanitize(product.description) %></p>
        <div class="price_line">
          <span class="price"><%= number_to_currency(product.price) %></span>
          <%= button_to t('.btn_add'), line_items_path(product_id: product), remote: true %>
        </div>
      </div>
  <% end %>